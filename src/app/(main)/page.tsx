export default function HomePage() {
  // This page is now redundant as its content has been moved to src/app/page.tsx
  // to resolve a route conflict. Returning null to prevent rendering.
  return null;
}
